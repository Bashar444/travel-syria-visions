

# Netlify redirects file for Single Page Application (SPA)
# Static files should be served directly, not redirected to React app

# Serve static files directly (these should NOT be redirected to index.html)
/robots.txt /robots.txt 200
/ads.txt /ads.txt 200
/favicon.ico /favicon.ico 200

# Serve uploaded images and assets directly
/lovable-uploads/* /lovable-uploads/:splat 200

# API routes (if any) - should be handled by external services
# /api/* https://api.example.com/:splat 200

# Force HTTPS (recommended for production)
http://syriantravel.in/* https://syriantravel.in/:splat 301!

# Redirect www to non-www (optional but recommended for SEO)
https://www.syriantravel.in/* https://syriantravel.in/:splat 301!

# SPA catch-all redirect - MUST be last rule
# This ensures that all other routes are handled by React Router
/* /index.html 200

